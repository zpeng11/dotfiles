#!/usr/bin/env bash
set -euo pipefail

case "$(uname -s)" in
  Darwin)
    exec "{{ .tools.nvim.macos.bin }}" "$@"
    ;;
  *)
    exec "$HOME/{{ .tools.nvim.linux.install_dir }}/bin/nvim" "$@"
    ;;
esac
