#!/usr/bin/env bash
set -euo pipefail

case "$(uname -s)" in
  Darwin)
    exec "{{ .tools.tmux.macos.bin }}" "$@"
    ;;
  *)
    APPIMAGE_EXTRACT_AND_RUN=1 exec "$HOME/{{ .tools.tmux.linux.install_dir }}/{{ .tools.tmux.linux.filename }}" "$@"
    ;;
esac
